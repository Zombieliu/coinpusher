# Alertmanager 配置

global:
  resolve_timeout: 5m
  # 邮件配置（可选）
  smtp_from: 'alertmanager@oops-moba.com'
  smtp_smarthost: 'smtp.gmail.com:587'
  smtp_auth_username: 'your-email@gmail.com'
  smtp_auth_password: 'your-app-password'

# 告警路由
route:
  receiver: 'default'
  group_by: ['alertname', 'category', 'severity']
  group_wait: 30s          # 等待30秒收集同组告警
  group_interval: 5m       # 每5分钟发送一次告警摘要
  repeat_interval: 4h      # 4小时后重复发送未解决的告警

  # 子路由
  routes:
    # 严重告警 - 立即通知
    - match:
        severity: critical
      receiver: 'critical-alerts'
      group_wait: 10s
      group_interval: 2m
      repeat_interval: 1h

    # 安全告警 - 发送到安全团队
    - match:
        category: security
      receiver: 'security-team'

    # 业务告警 - 发送到运营团队
    - match:
        category: business
      receiver: 'ops-team'

# 接收器配置
receivers:
  # 默认接收器（控制台）
  - name: 'default'
    webhook_configs:
      - url: 'http://host.docker.internal:8080/webhook/alerts'
        send_resolved: true

  # 严重告警（多渠道）
  - name: 'critical-alerts'
    # Webhook
    webhook_configs:
      - url: 'http://host.docker.internal:8080/webhook/critical'
        send_resolved: true

    # 邮件
    email_configs:
      - to: 'dev-team@oops-moba.com'
        headers:
          subject: '🚨 CRITICAL: {{ .GroupLabels.alertname }}'
        html: |
          <h2>🚨 Critical Alert</h2>
          <p><strong>Alert:</strong> {{ .GroupLabels.alertname }}</p>
          <p><strong>Severity:</strong> {{ .GroupLabels.severity }}</p>
          <p><strong>Summary:</strong> {{ .CommonAnnotations.summary }}</p>
          <p><strong>Description:</strong> {{ .CommonAnnotations.description }}</p>
          <p><strong>Time:</strong> {{ .StartsAt }}</p>

    # Slack（可选）
    # slack_configs:
    #   - api_url: 'https://hooks.slack.com/services/YOUR/WEBHOOK/URL'
    #     channel: '#alerts-critical'
    #     title: '🚨 {{ .GroupLabels.alertname }}'
    #     text: '{{ .CommonAnnotations.description }}'

  # 安全团队
  - name: 'security-team'
    webhook_configs:
      - url: 'http://host.docker.internal:8080/webhook/security'
        send_resolved: true
    email_configs:
      - to: 'security@oops-moba.com'

  # 运营团队
  - name: 'ops-team'
    webhook_configs:
      - url: 'http://host.docker.internal:8080/webhook/ops'
        send_resolved: true
    email_configs:
      - to: 'ops@oops-moba.com'

# 抑制规则（避免告警风暴）
inhibit_rules:
  # 如果有critical告警，抑制warning
  - source_match:
      severity: 'critical'
    target_match:
      severity: 'warning'
    equal: ['alertname', 'category']

  # 如果服务宕机，抑制性能告警
  - source_match:
      alertname: 'ServiceDown'
    target_match_re:
      category: 'performance|business'
